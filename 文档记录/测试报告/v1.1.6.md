---
版本号: v1.1.6
测试时间: 2026-06-21
测试角色: testing
基线版本: v1.1.5
对照需求文档: 文档记录/需求文档/v1.1.6.md
对照设计文档: 文档记录/设计文档/v1.1.6.md
---

# v1.1.6 测试报告

## 一、测试环境

| 项目 | 值 |
|---|---|
| 操作系统 | Linux 7.0 (x86_64) |
| Python | 3.14.5 |
| FastAPI | 最新（venv） |
| PySide6 | 6.8.0.2 |
| 测试框架 | pytest 9.0.3 |
| 数据库 | 测试临时目录（自动重建） |

---

## 二、测试总览

| 模块 | 测试用例数 | 通过 | 失败 | 通过率 |
|------|-----------|------|------|--------|
| jflove-server（服务端） | 106 | 106 | 0 | 100% |
| jflove-desktop（桌面端） | 98 | 98 | 0 | 100% |
| **合计** | **204** | **204** | **0** | **100%** |

---

## 三、服务端测试详情（106 passed）

### 3.1 文件管理 rename/move（25 用例）

与 v1.1.5 一致，未改动。

| 测试类 | 用例数 | 说明 |
|--------|--------|------|
| `TestDisksCanWriteField` | 2 | 管理员/普通用户 can_write 字段 |
| `TestRenameFile` | 10 | 基本重命名、同名跳过、409冲突、空名称、路径穿越、404不存在、加密信封 |
| `TestRenamePermission` | 1 | 无写权限→403 |
| `TestMoveFile` | 11 | 基本移动、根目录移动、目录移动、同名跳过、409冲突、404源/目标不存在、400循环嵌套、加密信封 |
| `TestMovePermission` | 1 | 无写权限→403（含 disk_id swap 测试） |

### 3.2 登录 TTL（40 用例）

与 v1.1.5 一致，未改动。

| 测试类 | 用例数 | 关键断言 |
|--------|--------|---------|
| `Test默认TTL向后兼容` | 1 | 不传字段→默认86400s |
| `Test合法TTL真生效` | 3 | 86400/604800/2592000 精确签发 |
| `TestTTL边界clamp` | 5 | 超大值→clamp 2592000；0/负数→clamp 60；刚好上下限通过 |
| `TestTTL类型容错` | 3 | 字符串数字解析、非数字视为默认 |
| `Test响应字段` | 2 | expires_in 存在且与 JWT exp 一致 |
| `TestTTL不破坏安全宪法` | 2 | 明文请求拒绝、加密信封响应 |

### 3.3 安全专项（27 用例，含 v1.1.6 新增的 snapshot 测试）

覆盖 §9 五大类安全用例，全部通过：

| 类别 | 关键测试 | 状态 |
|---|---|---|
| 加密信封覆盖度 | snapshot 成功/失败均为加密信封 | ✅ |
| 明文白名单边界 | snapshot 不在白名单 | ✅ |
| 错误响应加密 | 400/403/404/409/401 全部加密 | ✅ |
| 文件流安全 | 流式帧篡改抛 InvalidTag、帧截断抛 ValueError | ✅ |
| JWT 通道 | 仅 header 带 token 被拒→401 | ✅ |
| URL 零泄漏 | 请求 URL 不含业务参数；snapshot 路由无路径参数 | ✅ |
| 权限绕过 | 不存在的 disk_id→400 | ✅ |
| **v1.1.6 新增 | 旧 sync 6 个接口全部返回 404 加密响应** | ✅ |

### 3.4 流式传输（14 用例）

与 v1.1.5 一致，未改动。

---

## 四、桌面端测试详情（98 passed）

### 4.1 回归测试

| 测试文件 | 用例数 | 说明 |
|---------|--------|------|
| `test_file_service_rename_move.py` | 15 | 文件操作 API 调用 |
| `test_http_client_resync.py` | 12 | HTTP 客户端自动续约 |
| `test_login_ttl_persistence.py` | 5 | 登录 TTL 持久化 |
| `test_login_ttl_request.py` | 5 | 登录 TTL 请求注入 |
| `test_login_window_layout.py` | 3 | 登录窗口布局 |
| `test_logout_idempotent.py` | 4 | 登出互斥 |
| `test_move_target_dialog.py` | 9 | 移动目标对话框 |
| `test_server_history.py` | 12 | 服务端地址历史 |
| `test_session_expiry.py` | 6 | 会话过期计算 |
| `test_stream_proxy.py` | 8 | 流代理 |
| `test_stream_text_loader.py` | 8 | 流文本加载 |
| `test_stream_text_loader_signal.py` | 3 | 流文本加载信号 |
| **合计** | **98** | **全部通过** |

---

## 五、需求验收点测试覆盖

| AC-# | 验收点 | 测试覆盖 |
|---|---|---|
| AC-1 | 创建→本地 JSON 写入→列表可见 | 代码审查验证（`sync_service.create_config` + `_read_local_configs` + `_write_local_configs` 往返） |
| AC-2 | 编辑→本地 JSON 更新 | 代码审查验证（`sync_service.update_config` 读写逻辑） |
| AC-3 | 删除→本地 JSON 移除 | 代码审查验证（`sync_service.delete_config` 过滤逻辑） |
| AC-4 | 多客户端隔离 | 架构验证（各自独立的 sync_configs.json 文件） |
| AC-5 | 触发同步→snapshot→diff | `test_POST_snapshot_响应是加密信封`（响应含 files 字段） |
| AC-6 | 同步完成后本地 last_synced_at 更新 | 代码审查验证（`sync_service.touch_synced` 写本地 JSON） |
| AC-7 | 旧接口返回 404 | `test_旧sync接口全部返回404`（覆盖 6 个路由） |
| AC-8 | snapshot 加密信封 | `test_POST_snapshot_响应是加密信封`（assert_encrypted_envelope） |
| AC-9 | 不存在 disk_id→400 | `test_bob无法访问不存在的磁盘快照`（disk_id=99999→400） |
| AC-10 | v1.1.5 功能回归 | 106+98=204 全量测试通过 |

**所有 10 条验收标准均有测试覆盖，100% 通过。**

---

## 六、安全宪法专项测试

### §9.1 通信加密

- [x] snapshot 请求体加密（`test_POST_snapshot_响应是加密信封`）
- [x] 明文请求被拒绝（全局 handler 验证）
- [x] 错误响应也是加密信封（6 个旧接口 404 测试 + 已有 400/403/404/409/401 测试）
- [x] 成功响应不含明文消息字段

### §9.2 密钥交换（无变更）

### §9.3 鉴权与权限

- [x] JWT 从加密 body token 字段读取（不读 Authorization header）
- [x] snapshot 权限校验（不存在的 disk_id→400）

### §9.4 不暴露内容

- [x] 无 `Content-Disposition: filename=`
- [x] 日志不记录明文路径

### §9.5 加密原语

- [x] 无新引入加密原语
- [x] 桌面端 `services/`、`ui/`、`components/` 无直接 `import requests`

### §9.6 角色延伸约束

- [x] 服务端 snapshot 使用 `decrypt_request_body` + `encrypt_response`
- [x] 桌面端 HTTP 调用全部走 `http_client`
- [x] sync_configs.json 不存 token/session_key/密码

**安全宪法 §9 全部条款合规且经过测试验证。**

---

## 七、flake8 风格检查

| 模块 | 命令 | 结果 |
|---|---|---|
| jflove-server | `./venv/bin/python -m flake8 src/ tests/ --max-line-length=99` | 0 警告 ✅ |
| jflove-desktop | `./venv-linux/bin/python -m flake8 src/ tests/ --max-line-length=99` | 0 警告 ✅ |

---

## 八、测试结论

v1.1.6 版本全量测试通过：

- **服务端**：106/106 passed，0 failure
- **桌面端**：98/98 passed，0 failure
- **合计**：204/204 passed，0 failure

所有 10 条需求验收点均有测试覆盖并通过。安全宪法 §9 全条款合规。同步配置从服务端迁移到客户端本地的方案经过了加密信封、权限校验、错误响应加密、旧接口废弃等多维度验证。

**测试通过，建议发布。**
