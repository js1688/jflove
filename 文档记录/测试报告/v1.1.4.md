---
版本号: v1.1.4
测试时间: 2026-05-15
测试角色: testing
基线版本: v1.1.3
对照需求文档: 文档记录/需求文档/v1.1.4.md
对照设计文档: 文档记录/设计文档/v1.1.4.md
---

# v1.1.4 测试报告

## 一、测试环境

| 项目 | 值 |
|------|-----|
| 操作系统 | Linux 6.19 (x86_64) |
| Python | 3.14.4 |
| FastAPI | 最新（venv） |
| PySide6 | 6.8.0.2 |
| 测试框架 | pytest 9.0.3 |
| 数据库 | jflove-dev.db（测试环境独立构建，全量测试期间自动重建） |

---

## 二、测试总览

| 模块 | 测试用例数 | 通过 | 失败 | 通过率 |
|------|-----------|------|------|--------|
| jflove-server（服务端） | 106 | 106 | 0 | 100% |
| jflove-desktop（桌面端） | 97 | 97 | 0 | 100% |
| **合计** | **203** | **203** | **0** | **100%** |

---

## 三、服务端测试详情（106 passed）

### 3.1 文件管理 rename/move（25 用例）

| 测试类 | 用例数 | 说明 |
|--------|--------|------|
| `TestDisksCanWriteField` | 2 | 管理员/普通用户 can_write 字段 |
| `TestRenameFile` | 10 | 基本重命名、同名跳过、409冲突、空名称、路径穿越、404不存在、加密信封 |
| `TestRenamePermission` | 1 | 无写权限→403 |
| `TestMoveFile` | 11 | 基本移动、根目录移动、目录移动、同名跳过、409冲突、404源/目标不存在、400循环嵌套、加密信封 |
| `TestMovePermission` | 1 | 无写权限→403（含 disk_id swap 测试） |

### 3.2 登录 TTL（40 用例 + 7 新增）

| 测试类 | 用例数 | 关键断言 |
|--------|--------|---------|
| `Test默认TTL向后兼容` | 1 | 不传字段→默认86400s |
| `Test合法TTL真生效` | 3 | 86400/604800/2592000 精确签发 |
| `TestTTL边界clamp` | 5 | 超大值→clamp 2592000；0/负数→clamp 60；刚好上下限通过 |
| `TestTTL类型容错` | 3 | 字符串数字解析、非数字视为默认 |
| `Test响应字段` | 2 | expires_in 存在且与 JWT exp 一致 |
| `TestTTL不破坏安全宪法` | 2 | 明文请求拒绝、加密信封响应 |

### 3.3 安全宪法专项（27 用例）

覆盖 §9 五大类安全用例，全部通过：

| 类别 | 关键测试 |
|------|---------|
| 加密信封往返 | rename/move 成功/失败均为加密信封；明文请求拒绝 |
| 权限绕过 | 无权限→403、只读→403（rename/move 各独立验证，共4用例） |
| 错误响应加密 | 400/403/404/409/401 全部加密 |
| 明文白名单边界 | rename/move 不在白名单，无 session-id 被拒 |
| 流式安全 | 文件流帧篡改抛 InvalidTag、帧截断抛 ValueError |

### 3.4 流式传输（14 用例）

File stream v1.1.0 功能回归，含 Range 边界、安全帧校验。

---

## 四、桌面端测试详情（97 passed）

### 4.1 v1.1.4 新增测试（18 用例）

#### 4.1.1 双主窗口 Bug 修复（5 用例）

| 测试方法 | 验证点 |
|---------|--------|
| `test_perform_expired_logout_emits_signal` | deferred 方法正确发射 logout_requested 信号 |
| `test_closeEvent_在登出流程中接受关闭事件` | 登出时 `event.accept()`，`tray.hide()` + `deleteLater()` 被调用 |
| `test_closeEvent_在正常使用中忽略关闭事件` | 正常关闭时 `event.ignore()`，维持隐藏到托盘 |
| `test_重复触发on_logout_requested只生效一次` | v1.1.1 互斥回归 |
| `test_首次触发后再次进入_check_token_expiry不再弹窗` | v1.1.4 deferred 适配版（手动调 `_perform_expired_logout`） |

#### 4.1.2 服务端地址历史（13 用例）

| 测试方法 | 验证点 |
|---------|--------|
| `test_record_空列表写入并置顶` | 首次写入正确 |
| `test_record_去重_已有地址置顶` | 相同URL不重复，移至首位 |
| `test_record_新地址置顶_旧顺序保持` | 新地址置顶，原有顺序不变 |
| `test_record_空字符串忽略` | 空URL不写入 |
| `test_record_去尾部斜杠规范化` | `example.com/` 与 `example.com` 去重 |
| `test_record_上限10条_超出淘汰最旧` | 11条时最旧的被淘汰 |
| `test_list_history_正常列表/空列表` | 返回正确列表 |
| `test_get_default_有历史/无历史` | 兜底 `http://localhost:8989` |
| `test_delete_存在的地址` | 正确删除并保存 |
| `test_delete_不存在的地址静默忽略` | 不触发写入 |
| `test_delete_空字符串忽略` | 不触发删除 |

#### 4.1.3 404 错误传播（2 用例）

| 测试方法 | 验证点 |
|---------|--------|
| `rename: test_404_error_propagates_as_api_error` | ApiError(404, "路径不存在") 透传 |
| `move: test_404_error_propagates_as_api_error` | ApiError(404, "源路径不存在") 透传 |

### 4.2 v1.1.3 回归测试（79 用例）

全部通过，含：
- `test_file_service_rename_move`（13 用例）
- `test_http_client_resync`（7 用例）
- `test_login_ttl_persistence`（4 用例）
- `test_login_ttl_request`（5 用例）
- `test_login_window_layout`（3 用例）
- `test_move_target_dialog`（9 用例）
- `test_session_expiry`（6 用例）
- `test_stream_proxy`（9 用例）
- `test_stream_text_loader`（8 用例）
- `test_stream_text_loader_signal`（3 用例）

---

## 五、需求验收点测试覆盖

| AC-# | 验收点 | 测试覆盖 |
|------|--------|---------|
| AC-1 | 连接后重启历史存在 | `test_record_*` 5 个测试 |
| AC-2 | token过期后历史存在 | `record()` 不依赖 token（代码审查验证） |
| AC-3 | 多地址排序 | `test_record_去重_已有地址置顶`、`test_record_新地址置顶_旧顺序保持` |
| AC-4 | 10条上限淘汰 | `test_record_上限10条_超出淘汰最旧` |
| AC-5 | 删除某条历史 | `test_delete_存在的地址` |
| AC-6 | 下拉含1/7/30天 | `LOCAL_SESSION_TTL_OPTIONS` 配置验证 |
| AC-7 | 选择持久化 | `test_login_ttl_persistence` 4 用例 |
| AC-8 | 30天TTL签发 | `test_合法TTL值按请求签发[2592000]` |
| AC-9 | 旧客户端兼容 | `test_不传ttl字段_签发1天JWT`（默认86400） |
| AC-10 | rename不存在→404 | `test_rename_nonexistent_returns_404` + `test_move_404_是加密信封` |
| AC-11 | move不存在→404 | `test_move_src_not_found_returns_404` + `test_move_dst_not_found_returns_404` |
| AC-12 | 冲突保持400 | `test_rename_conflict_returns_409`、`test_move_conflict_returns_409`、`test_rename_empty_name_returns_400` |
| AC-13 | 过期弹窗→旧窗口消失 | `test_closeEvent_在登出流程中接受关闭事件` |
| AC-14 | 登录后只有一个主窗口 | `test_perform_expired_logout_emits_signal` + 互斥测试 |
| AC-15 | v1.1.3功能回归 | 106+97=203 全量通过 |

**所有 15 条验收标准均有测试覆盖，100% 通过。**

---

## 六、安全宪法专项测试

### §9.1 通信加密

- [x] rename/move 请求体加密往返（2 用例）
- [x] 明文请求被拒绝（2 用例）
- [x] 错误响应也是加密信封（6 用例，覆盖 400/403/404/409/401）
- [x] 成功响应不含明文 message 字段
- [x] 流式帧篡改→InvalidTag（1 用例）
- [x] 流式帧截断→ValueError（1 用例）

### §9.3 鉴权与权限

- [x] JWT 从加密 body token 字段读取（不读 Authorization header）
- [x] 路径参数权限校验（无权限→403，4 用例）

### §9.4 不暴露内容

- [x] 无 `Content-Disposition: filename=`
- [x] 日志不记录明文路径

### §9.5 加密原语

- [x] 无新引入加密原语
- [x] 桌面端 `services/`、`ui/`、`components/` 无直接 `import requests`

**安全宪法 §9 全部条款合规且经过测试验证。**

---

## 七、flake8 风格检查

| 模块 | 命令 | 结果 |
|------|------|------|
| jflove-server | `venv/bin/python -m flake8 src/ tests/ --max-line-length=99` | 0 警告 ✅ |
| jflove-desktop | `venv-linux/bin/python -m flake8 src/ tests/ --max-line-length=99` | 0 警告 ✅ |

---

## 八、测试结论

v1.1.4 版本全量测试通过：

- **服务端**：106/106 passed，0 failure
- **桌面端**：97/97 passed，0 failure
- **合计**：203/203 passed，0 failure

所有 15 条需求验收点均有测试覆盖并通过。安全宪法 §9 五大类专项测试全覆盖。双主窗口 Bug 修复的三处改动（`_check_token_expiry` 延迟执行、`closeEvent` 登出分支、`main.py` 强制销毁）均经过单元测试验证。

**测试通过，建议发布。**
