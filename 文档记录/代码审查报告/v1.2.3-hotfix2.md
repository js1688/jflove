# 代码审查报告 — v1.2.3 第三轮 BUG 修复

- **审查日期**：2026-07-27
- **审查范围**：移动端 6 个 BUG 修复涉及的 6 个文件
- **版本号**：v1.2.3（BUG 修复，版本号不变）

## 审查范围

| 文件 | 变更 |
|------|------|
| `lib/services/transfer_service.dart` | 修复上传三步协议 + SHA256 哈希 |
| `lib/providers/transfer_provider.dart` | 流立即发射初始数据 |
| `lib/pages/files/disk_browser_page.dart` | 下载路径改为外部存储 + 面包屑集成到 AppBar |
| `lib/widgets/path_breadcrumb.dart` | 紧凑单行路径显示 |
| `lib/services/sync_engine_service.dart` | 注入 TransferService，实现实际传输 |
| `lib/providers/sync_provider.dart` | 注入 TransferService 到 SyncEngineService |
| `android/app/src/main/AndroidManifest.xml` | 应用名改为 JFLove |

## 总体评价

所有 6 个 BUG 修复均正确实现，无严重或中等问题。代码符合 AGENTS.md 编码规范，安全宪法各项均满足。

## 问题列表

### 严重 — 0 个

无。

### 中等 — 0 个

无。

### 轻微 — 0 个

无。

## 安全宪法逐条核查

| 条款 | 状态 | 说明 |
|------|------|------|
| §9.1 加密通道 | ✅ | 所有 API 调用均走 `_http.encryptedPost/Get/Delete`，未发现明文请求 |
| §9.2 密钥交换 | ✅ | 未引入长期密钥/持久化 session_key |
| §9.3 鉴权与权限 | ✅ | JWT 通过 `_buildPayload` 中的 `token` 字段传递 |
| §9.4 不暴露内容 | ✅ | 响应中不含文件名/path/用户标识；下载路径仅显示在客户端 UI |
| §9.5 加密原语 | ✅ | SHA256 通过 pointycastle 原语计算，未引入新算法 |
| §9.6 角色约束 | ✅ | HTTP 调用全部走 `HttpService`；未引入 ffi/native 加密库 |
| §9.7 新 API 自查清单 | ✅ | upload/init → upload/chunk → upload/complete 三步协议均走加密通道 |

## 设计一致性

- 上传协议与 `jflove-server/src/controllers/file_controller.py` 完全一致（init → chunk → complete）
- 同步引擎与 `jflove-desktop/src/utils/sync_engine.py` 对齐（差异对比 + 实际传输）
- 传输任务流与 `jflove-desktop/src/utils/transfer_manager.py` 对齐（立即返回当前状态）

## 总结

所有变更安全、合规，通过审查。可进入测试阶段。
