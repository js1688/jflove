---
版本号: v1.3.1
发布时间: 2026-08-02
发布角色: devops
基线版本: v1.3.0
发布范围: 服务端 + Web 端 + 移动端（桌面端除外）
对照代码审查报告: 文档记录/代码审查报告/v1.3.0.md（附录 B 方案 B 审查 ✅）
对照测试报告: 文档记录/测试报告/v1.3.0.md（附录 B 方案 B 测试 ✅）
---

# v1.3.1 版本发布记录：服务端 + Web 端 + 移动端版本号升级

> 本版本为 v1.3.0 的**增量发布**：服务端、Web 端、移动端版本号统一升级为 1.3.1，
> 桌面端无变更、不参与本次发布。

## 一、发布范围

| 模块 | 变更 | 发布产物 |
|------|------|---------|
| **jflove-server** | 版本号 1.3.0 → **1.3.1**（承载方案 B middleware：GET 加密参数支持 body + URL query 双通道） | Docker 镜像 `jflove-server:1.3.1` ✅ |
| **jflove-web** | 版本号 1.3.0 → **1.3.1**（方案 B：GET 加密参数 URL 传递、405 修复、地址规范化；**UI 调整**：首页移除、移动视图对齐安卓 App、顶部 sticky；**HTTP 兼容**：纯 JS 加密回退；**文件预览**：视频/音频支持 + **Service Worker 流式代理**边下边播+seek） | Docker 镜像 `jflove-web:1.3.1` ✅ |
| **jflove-app** | 版本号 1.2.3+3 → **1.3.1+3**（移动端版本号对齐） | APK（**❌ 环境未就绪**，见下文） |
| jflove-desktop | 无变更 | — |

> **移动端发布产物说明**：移动端（Flutter 应用）发布产物为 **APK**（`flutter build apk --debug` + `flutter build apk`），**不是 Docker 镜像**——Flutter 移动应用不发布容器镜像。服务端 / Web 端才是 Docker 镜像发布。

## 二、版本号核查（发布阻塞项）

| 模块 | 字段 | 期望 | 实际 | 状态 |
|------|------|------|------|------|
| 服务端 | `src/main.py` `FastAPI(version=)` | 1.3.1 | 1.3.1 | ✅ |
| 服务端 | `build.py` `VERSION` | 1.3.1 | 1.3.1 | ✅ |
| 服务端 | `Dockerfile` `LABEL version=` | 1.3.1 | 1.3.1 | ✅ |
| Web 端 | `package.json` `version` | 1.3.1 | 1.3.1 | ✅ |
| Web 端 | `build.py` `VERSION` | 1.3.1 | 1.3.1 | ✅ |
| 移动端 | `pubspec.yaml` `version` | 1.3.1+3 | 1.3.1+3 | ✅ |

6 处全部一致 ✅（服务端 3 + Web 端 2 + 移动端 1；桌面端不参与）

## 三、发布技能完善（devops）

- 核对 `jflove-web/build.py`：**确认 Web 端发布流程为 Docker 镜像**（`python build.py` → `jflove-web:<version>`，多阶段构建 nginx serve），技能已具备，无需新增。
- **补充技能缺口**：`devops/SKILL.md` 版本号核查清单新增 `jflove-web/build.py VERSION`（Web 端共 2 处版本号，原清单仅列 package.json）。

## 四、构建产物

| 交付物 | 校验 | 说明 |
|--------|------|------|
| `jflove-server:1.3.1` / `latest` | 构建成功 ✅ | Docker 镜像（含方案 B middleware），prod.db 空表结构校验通过 |
| `jflove-web:1.3.1` | 构建成功 ✅（**最终构建**，含纯 JS 加密回退 + 视频/音频预览 + SW 流式代理） | Docker 镜像（nginx SPA + `dist/sw.js`），manifest `5ff567a6a1e1`，本地构建 |
| 移动端 APK | ❌ 未构建 | 构建环境未就绪（见五） |

## 五、移动端构建环境（❌ 未就绪）

| 检查项 | 结果 |
|--------|------|
| `flutter` 命令 | ❌ 不存在（当前 Linux 环境未安装 Flutter SDK；AGENTS.md 约定的 `FLUTTER_HOME` 为 Windows 路径 `D:\flutter\flutter_3.44.6-stable`） |
| Android SDK | ❌ 未检查（依赖 flutter） |

**需在具备 Flutter 的环境执行**（版本号已就绪 1.3.1+3）：

```bash
cd jflove-app
flutter pub get
flutter build apk --debug   # → build/app/outputs/flutter-apk/app-debug.apk
flutter build apk           # → build/app/outputs/flutter-apk/app-release.apk
# 两个 APK 均需真机安装并完成：启动 → 登录 → 文件浏览 → 笔记编辑
```

> 发布前需补做移动端 R8 检查（`android/app/build.gradle.kts` release 中 `isMinifyEnabled` / `isShrinkResources` 必须为 false）。

## 六、冒烟测试结果（2026-08-02）

| 检查项 | 结果 |
|--------|------|
| 服务端镜像构建 | ✅ `jflove-server:1.3.1` 构建成功 |
| 服务端容器 `/health` | ✅（v1.3.0 附录 B 容器冒烟已验证；本次仅版本号变更，无代码变更） |
| 服务端 query 信封中间件 | ✅（v1.3.0 附录 B 容器冒烟验证：query 信封正常 + 错误响应加密） |
| Web 镜像构建 | ✅ `jflove-web:1.3.1` 最终构建成功（manifest `5ff567a6a1e1`，含 SW 流式代理） |
| Web 容器首页 | ✅ HTTP 200（登录页渲染，右下角版本号 **v1.3.1**） |
| Web 容器 `/sw.js` | ✅ HTTP 200（module SW，import `./assets/stream-frame-*.js`） |
| Web 容器 SPA fallback | ✅ `/files`、`/settings`、`/files/2/preview` 均 200（不 404） |
| 生产构建（Vite） | ✅ dist/ 生成成功（含 sw.js，Docker Stage 1） |
| Web 端自测 | ✅ tsc 0 / eslint 0 / vitest 43 |
| HTTP 域名兼容 | ✅ 纯 JS 加密回退与后端互操作实测通过（key-exchange/login/加密 GET） |
| **SW 流式播放（HTTPS/localhost）** | ✅ 实测：`/jflove-stream/{token}` → SW 返回 206 + 解密 MP4 流，`readyState=4`、460s、852×480；**seek 到 300s 成功**（后端多次 Range 请求） |
| 视频预览（回退） | ✅ HTTP 非安全上下文自动回退 Blob 预览 |
| SPA fallback | ✅ nginx `try_files $uri $uri/ /index.html`（v1.3.0 附录 B 容器冒烟已验证） |
| 功能链路 | ✅ v1.3.0 附录 B 已全链路复验（登录/文件/笔记/设置/管理/同步/传输） |

## 七、安全宪法检查

| 检查项 | 结果 |
|--------|------|
| `_PLAIN_PATHS` 白名单 | 未扩大（仍 3 项）✅ |
| `_session_store` 内存字典 | ✅ |
| 加密协议版本 | `X-Encrypted-Stream: v1`（不变）✅ |
| URL 无明文参数 | ✅（方案 B 已审查/测试） |
| nginx.conf SPA fallback | ✅ |
| **SW 流式代理** | ✅ session_key 经 postMessage 内存传递、不落盘；token 一次性随机防猜测；SW 仅拦截 `/jflove-stream/` 前缀 |

> **部署要求**：Service Worker 仅安全上下文（HTTPS / localhost）可注册，生产需 **HTTPS** 部署以启用视频/音频边下边播；HTTP 域名访问自动回退 Blob（功能仍可用）。

## 八、部署步骤

```bash
# 服务端（挂载持久化 + 物理盘；Fedora/SELinux 需加 :Z）
docker run -d --name jflove-server -p 8989:8989 \
  -v /your/data:/data:Z -v /mnt/your-big-disk:/storage:Z \
  --restart=always jflove-server:1.3.1

# Web 端
docker run -d --name jflove-web -p 8080:80 --restart=always jflove-web:1.3.1
# 镜像推送到仓库由人工执行：docker push <registry>/jflove-server:1.3.1 / <registry>/jflove-web:1.3.1
```

## 九、回滚预案

| 场景 | 操作 |
|------|------|
| 服务端异常 | 回滚 `jflove-server:1.3.0`（版本号变更，代码与 1.3.0 一致；Web 端 1.3.1 依赖 query 信封，需与服务端 1.3.0 或 1.3.1 搭配） |
| Web 端异常 | 回滚 `jflove-web:1.3.0`（上一镜像） |
| 移动端 | 版本号回滚为 1.2.3+3（未发布 APK，无实际影响） |
| 数据库 | 无 DDL 变更，无需回滚 |

---

# 补充发布记录（2026-08-04）：Web 端「边下边播」+ 流式 404 修复（同版本 v1.3.1）

> 本补充为 **v1.3.1 版本内** Web 端代码 BUG 修复 + 功能完善（AGENTS.md §7.5.3 快速修复通道，
> **版本号不变**）。2026-08-02 记录为版本号升级发布；本次为边下边播功能代码落地与 404 修复。

## 一、发布范围

| 模块 | 变更 | 发布产物 |
|------|------|---------|
| **jflove-web** | ① 视频/音频**真边下边播**：Service Worker 流式代理（`/jflove-stream/<token>`，Range→后端 `/stream`→逐帧解密→206，原生解码）为主路径，MSE 回退（非安全上下文），完整下载仅兜底；② 修复流式 `/stream` 404「文件不存在」（`openEncryptedStream` path 归一化）；③ 修复 `parseStreamFrames` 单分片丢帧 | **仅发布文档**（见四） |
| jflove-server / desktop / app | 无变更 | — |

## 二、版本号核查（发布阻塞项）

| 模块 | 字段 | 期望 | 实际 | 状态 |
|------|------|------|------|------|
| Web 端 | `package.json` `version` | 1.3.1 | 1.3.1 | ✅ |
| Web 端 | `build.py` `VERSION` | 1.3.1 | 1.3.1 | ✅ |
| Web 端 | `src/config/constants.ts` `APP_VERSION` | 1.3.1 | 1.3.1 | ✅ |

Web 端 3 处全部一致 ✅（本轮仅 Web 端发布；服务端/桌面端/移动端版本号未变、不参与本轮发布）

## 三、发布前 checklist

- [x] 代码审查报告 `v1.3.1.md`：无未解决严重问题（§9 逐条核查通过）
- [x] 测试报告 `v1.3.1.md`：51 通过，5 类安全用例全覆盖
- [x] 开发记录已归档：`Web端开发记录/v1.3.1.md`（第七、八轮）；计划已归档 `plans/归档/DONE-web-streaming-preview.md`
- [x] 生产库：无 DDL 变更（后端无改动），无需同步表结构
- [x] 安全宪法 §9 发布清单：`_PLAIN_PATHS` 未扩大（仍 3 项）；`_session_store` 仍内存字典；加密协议 `X-Encrypted-Stream: v1/v2` 不变；流式 URL 无明文参数（opaque token）；nginx SPA fallback 兼容 `/sw.js`

## 四、构建与环境（⚠️ 按用户指示不实际构建）

- **环境检查**：`docker version` → **❌ Docker 不可用（当前环境无 Docker daemon）**。
- **按用户明确指示**：本轮**不执行实际的 Docker 镜像构建/打包**，仅产出发布文档资料。
- 需在具备 Docker 的环境补充执行（版本号已就绪）：

```bash
cd jflove-web
npm install                 # 补装 @noble/hashes、@noble/curves（若缺失）
npx vite build              # 产物 dist/（含 dist/sw.js 模块 SW + 共享 chunk）
python build.py             # → Docker 镜像 jflove-web:1.3.1（可选 --save 导出离线包）
docker run -d --name jflove-web -p 8080:80 --restart=always jflove-web:1.3.1
# 冒烟：首页 200、/files SPA fallback 200、/sw.js 200
```

## 五、交付物（本轮产出）

| 交付物 | 路径 | 说明 |
|--------|------|------|
| 开发记录 | `文档记录/Web端开发记录/v1.3.1.md` | 第七轮（边下边播方案+实现）、第八轮（404 修复+单分片丢帧） |
| 代码审查报告 | `文档记录/代码审查报告/v1.3.1.md` | §9 逐条核查，无严重项 |
| 测试报告 | `文档记录/测试报告/v1.3.1.md` | vitest 51 通过 + 真实服务器 E2E |
| 项目管理记录 | `文档记录/项目管理记录/v1.3.1.md` | 任务/里程碑/风险/交付物核验/README 核验 |
| 发布记录 | 本文件（补充章节） | 版本号一致、环境未就绪（Docker ❌） |
| 根 README | `README.md` | 版本变化日志补 v1.3.1（当前版本） |

## 六、验证结果（真实环境，2026-08-04）

- 服务器 `server.jflove.cn:8989`、磁盘(500GB) → 叶方 → `IMG_0443.MP4`（15.2MB）：
  - SW 流式代理生效（video.src = `/jflove-stream/<token>`），`readyState=3`、`duration=45.66s`、无 404 / 无 error ✅
  - `video.play()` 进度前进 → **真边下边播** ✅
  - 拖动 seek 到 30s 成功（Range 生效）✅
- 全程仅访问磁盘(500GB)/叶方，未触碰磁盘(240GB)，未删除任何数据。
- 自测：`tsc` 0 / `eslint` 0 / `vitest` **51** 通过。

## 七、回滚预案（本轮）

| 场景 | 操作 |
|------|------|
| 边下边播异常（播放器不工作） | 前端代码回滚至上一提交（恢复 Blob 全量预览）；不影响登录/文件管理等功能 |
| 数据库 | 无 DDL 变更，无需回滚 |
| 服务端 | 无变更，无需回滚 |

## 八、部署要求

- **边下边播主路径（SW）仅安全上下文可用**：生产建议 HTTPS（localhost/127.0.0.1 亦可）；HTTP 域名自动走 MSE 回退（fMP4/WebM/mp3 等仍边下边播），两者都不可行才完整下载。
- Web 镜像构建依赖 Docker，当前环境无 Docker，已按用户指示跳过实际构建，仅保留本发布文档。
