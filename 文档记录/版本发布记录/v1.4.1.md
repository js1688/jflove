---
版本号: v1.4.1
发布时间: 2026-08-23
发布角色: devops
发布类型: BUG 快速修复通道（服务端 + Web 端 + 桌面端 + 移动端）
上一版本: v1.4.0
---

# v1.4.1 版本发布记录：视频播放修复（时长 + seek）

## 一、版本信息

| 模块 | 版本 | 变更 |
|------|------|------|
| jflove-server | **1.4.1** | 媒体探测失败回退 byte、ffmpeg 可用性修复；TS 伪装 MP4 强制转 AAC；faststart MP4 判定修正；init 段补读；**fMP4 mvhd duration 改写（供播放器显示总时长）** |
| jflove-web | **1.4.1** | MSE 非 fMP4 回退下载 + 下载进度；MSE 时长/seek 修复（timestampOffset + 非受控 src + 防竞态） |
| jflove-desktop | **1.4.1** | StreamProxy time 修复流声明 Accept-Ranges，使 QMediaPlayer 可拖拽 seek |
| jflove-app | **1.4.1** | StreamProxy time 修复流声明 Accept-Ranges，使 ExoPlayer 可拖拽 seek |

> 接口契约不变（`stream_mode` / `range_start_seconds` / meta 帧字段均不变），桌面/移动端仅 StreamProxy 一处改动。

### 修复明细（迭代内追加，均在 v1.4.1 范围内）

| 编号 | 现象 | 根因 | 修复 |
|------|------|------|------|
| BUG-4 | TS 伪装 .mp4（0x47 同步字节）无法 MSE 播放 | 源为 MPEG-TS 流，Chrome MSE 不识别 | ffmpeg 修复流 `-c:a aac` 强制转码音频 + 输出 fMP4 |
| BUG-5 | 修复后无声音 / 报 codec 不识别 | 某些源 mp4a 无 DecoderSpecificInfo | `_mp4a_codec` 回退 `mp4a.40.2` + Web codec 串补全 |
| BUG-6 | faststart MP4（moov 前置但无 mvex）MSE 卡死 | faststart≠fMP4，MSE 需要 moov+mvex | 服务端 `_moov_at_front` 要求 mvex；Web `isFmp4Init` 同判定，非 fMP4 回退下载 |
| BUG-7 | 视频时长未知、无法拖进度、点击进度条播放失败 | fMP4 `empty_moov` 无时长信息；seek 后旧流退出把 endOfStream 写到新流 | Web 用服务端 meta.duration 设 `MediaSource.duration`；consume 代际计数防竞态 |
| BUG-8 | 初始播放间歇性失败（空 codec / ftyp-only init 段） | ffmpeg 把 ftyp 与 moov 分两次写出，首块 `read` 只拿到 28 字节 ftyp → `parse_fmp4_codec` 解析为空 | 服务端 `_read_fmp4_init` 累加读取直到出现 moov（上限 4MB） |
| BUG-9 | seek 后旧流仍占服务端修复并发槽，新流排队超时失败 | Web 只 `frames.return()` 未真正 cancel 旧 fetch | Web 每流独立 AbortController，seek 前 abort 旧流；seek 改用 `SourceBuffer.timestampOffset` 保持总时长恒定、播放头不归零；`<video>` src 改为非受控避免与 MSE 命令式 src 冲突 |
| BUG-10 | 桌面/移动端 time 修复流无总时长、拖拽进度条无效 | empty_moov 的 mvhd duration=0 播放器拿不到时长；HEAD 不声明 Accept-Ranges 播放器认为不可 seek | 服务端改写 mvhd duration 提供总时长；两端 StreamProxy 声明 Accept-Ranges 使播放器可 seek |

> BUG-4/5/6/8/10 在服务端镜像中生效；BUG-7/9 在 Web 中生效；BUG-10 的客户端部分（Accept-Ranges）在桌面端 1.4.1 / 移动端 1.4.1 中生效。

## 二、交付物清单

| 交付物 | 镜像 ID | 大小 | 说明 |
|--------|---------|------|------|
| `jflove-server:1.4.1` | `fe5cafca13d5` | 1.26GB | 后端镜像（含 ffmpeg + TS 转 AAC + faststart mvex + init 段补读 + mvhd duration 改写） |
| `jflove-server:latest` | `fe5cafca13d5` | 1.26GB | 同上 |
| `jflove-web:1.4.1` | `b034686e75c4` | 75.6MB | Web 前端镜像（MSE 时长/seek 修复 + 非受控 src） |
| `jflove-desktop` | PyInstaller 单文件 | 242MB | `build/dist/JFLove.exe`，sha256 `ba3751956753e61719303cc2fbdac25d696cd10bc48a8fddf59288a98bd8e26b`（2026-08-24 构建） |
| `jflove-app` | APK debug + release | 169MB + 73MB | `app-debug.apk` sha256 `86f6c638…7d083`；`app-release.apk` sha256 `a5394a78…4d80553`（均为 1.4.1+5） |

> 服务端/Web 镜像均已推送 `ccr.ccs.tencentyun.com/jflove/`（server digest `sha256:fe5cafca13d5…`、web digest `sha256:b034686e75c4…`）。桌面端/移动端产物于 2026-08-24 在本机构建完成（详见下方「构建产物」）。

## 三、依赖变更

| 依赖 | 变更 | 说明 |
|------|------|------|
| 系统 ffmpeg（apt） | **新增** | Dockerfile `apt-get install ffmpeg`，供媒体修复使用 |
| imageio-ffmpeg | 保留（兜底） | `get_ffmpeg_exe` 优先系统 ffmpeg，退回 imageio-ffmpeg |
| 其余 Python / npm 依赖 | 无变更 | — |

## 四、生产库 DDL

**无 DDL 变更。** 本次仅代码 + 镜像层修复，`config` 表无新增键（`media_repair_*` 键为 v1.4.0 已初始化）。

## 五、代码审查 / 测试门禁

| 门禁 | 结果 |
|------|------|
| 代码审查 | ✅ 通过，无严重问题（`文档记录/代码审查报告/v1.4.1.md`） |
| 后端 pytest | ✅ 122 passed |
| 后端 flake8 | ✅ 0 警告 |
| Web vitest | ✅ 52 passed |
| Web lint / tsc | ✅ 0 警告 / 0 错误 |
| 桌面端 flake8 | ✅ 0 警告 |
| 桌面端 pytest | ✅ 102 passed（修复 1 个过时测试用例，见桌面端开发记录） |
| 移动端 `flutter analyze` | ✅ No issues found |
| 移动端 `flutter test` | ✅ 27 passed |
| 安全宪法 §9 | ✅ 未破坏（无新增白名单/路由/加密原语） |

## 六、冒烟结果

| 项 | 结果 |
|----|------|
| 服务端 `docker run` 启动 | ✅ `/health` → `{"status":"ok"}` |
| 服务端版本 | ✅ `/openapi.json` → `version: 1.4.1` |
| 镜像内 ffmpeg | ✅ `/usr/bin/ffmpeg`（7.1.5），`get_ffmpeg_exe()` 解析正确 |
| Web 首页 | ✅ HTTP 200 |
| Web JS bundle 版本 | ✅ 含 `1.4.1` |
| 桌面端 JFLove.exe | ✅ 构建产物存在（242MB），版本号一致性校验通过（build.py / settings.py 均 1.4.1） |
| 桌面端启动/关于框 | ⏳ 待用户本机运行验证（本机无 GUI 会话） |
| 移动端 app-debug.apk | ✅ 构建成功（169MB），`flutter analyze` + `flutter test` 通过 |
| 移动端 app-release.apk | ✅ 构建成功（73.0MB），versionName 继承 pubspec 1.4.1+5 |
| 移动端真机安装 | ⏳ 待用户真机验证（本机无 Android 真机/模拟器） |

## 七、部署步骤（用户侧）

### 服务端（192.168.100.5）

```bash
# 1. 推送镜像（本机 docker push 到仓库，或在 NAS 上重新构建）
docker tag jflove-server:1.4.1 <仓库>/jflove-server:1.4.1
docker push <仓库>/jflove-server:1.4.1

# 2. NAS 上拉取并重启（注意保留 -v /data 与 /storage 挂载）
docker pull <仓库>/jflove-server:1.4.1
docker rm -f jflove-server  # 先停旧容器
docker run -d --name jflove-server -p 8989:8989 \
  -v /opt/jflove/data:/data \
  -v /mnt/big-disk:/storage \
  --restart=always jflove-server:1.4.1
```

### Web 端

```bash
docker tag jflove-web:1.4.1 <仓库>/jflove-web:1.4.1
docker push <仓库>/jflove-web:1.4.1
# NAS 上
docker pull <仓库>/jflove-web:1.4.1
docker rm -f jflove-web
docker run -d --name jflove-web -p 18080:80 --restart=always jflove-web:1.4.1
```

### 桌面端（Windows）

```text
产物：jflove-desktop/build/dist/JFLove.exe（242MB，单文件免安装）
sha256：ba3751956753e61719303cc2fbdac25d696cd10bc48a8fddf59288a98bd8e26b
部署：拷贝到目标机，直接运行即可（内置 HTTP 代理，仅绑 127.0.0.1）
```

### 移动端（Android）

```text
产物：
  jflove-app/build/app/outputs/flutter-apk/app-debug.apk   （169MB，调试用）
  jflove-app/build/app/outputs/flutter-apk/app-release.apk （73MB，正式安装）
sha256：
  debug   = 86f6c6389e45eadea6680a656ca3509f5caf0da3a510e37dd37c288f09d7a083
  release = a5394a78bb52d7cb412200515391ba79d76de8f25e7974b913cbff1694d80553
版本：1.4.1+5（versionName 继承 pubspec.yaml）
安装：adb install -r app-release.apk 或直接拷贝 APK 到手机安装
```

### 部署后验证

1. 浏览器访问 Web，打开普通 MP4：应能**回退完整下载并显示「下载中 x%」后正常播放**（BUG-1 生效）。
2. 管理面板开启「媒体修复」后，普通 MP4 / MKV 应能**边下边播**（BUG-2/BUG-3 生效，ffmpeg 可用）。
3. 若 ffmpeg 仍不可用，修复开关开启后也**不再报 415**，自动回退 byte + 下载播放（BUG-2 兜底）。
4. TS 伪装 .mp4（「损坏的」视频）：开启媒体修复后应能**边下边播且有声**（BUG-4/BUG-5 生效）。
5. 修复流播放时进度条应**显示总时长、可拖拽 seek**；点击进度条跳转后应从目标点继续播放，不再「播放失败」（BUG-6/BUG-7 生效）。
6. **桌面端**：运行 `JFLove.exe`，窗口标题/关于对话框应显示版本号 `1.4.1`；打开 TS 伪装 MP4 确认进度条有总时长、可拖拽 seek（BUG-10 生效）。
7. **移动端**：`adb install -r app-release.apk`，启动 → 登录 → 文件浏览 → 笔记编辑 → 「关于」应显示 `1.4.1`；打开「损坏的」视频确认时长与 seek 正常（BUG-10 生效）。

## 八、回滚预案

- 服务端回滚：`docker run ... jflove-server:1.4.0`（旧镜像仍在本机/仓库）。
- Web 回滚：`docker run ... jflove-web:1.4.0` 或 `jflove-web:1.3.1`。
- 本次无 DDL / 配置变更，回滚无数据迁移成本。

## 九、加密协议版本

- 不变：`X-Encrypted-Stream: v2`（stream_file 流式预览）、`v1`（下载/预览旧接口）。
- 白名单仍为 3 条：`GET /health`、`POST /api/v1/auth/key-exchange`、`GET /api/v1/auth/admin-exists`。
- `_session_store` 仍为进程内存字典（PFS 保持）。
